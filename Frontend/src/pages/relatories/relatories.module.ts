import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RelatoriesPage } from './relatories';

@NgModule({
  declarations: [
    RelatoriesPage,
  ],
  imports: [
    IonicPageModule.forChild(RelatoriesPage),
  ]
})
export class RelatoriesPageModule {}
